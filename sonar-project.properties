sonar.organization=douglas-siiva
sonar.projectKey=Douglas-SiIva_crimp

sonar.sources=src,include,spike

# main.c is thin CLI glue (argv parsing, printf formatting) that wraps
# libcrimp orchestration (crimp_scan_directory, tested in tests/core/test_scan.c)
# — validated end-to-end manually rather than via automated coverage.
sonar.coverage.exclusions=src/cli/main.c
