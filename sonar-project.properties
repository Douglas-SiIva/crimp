sonar.organization=douglas-siiva
sonar.projectKey=Douglas-SiIva_crimp

# src/include/tests don't exist yet (nothing real committed there yet).
# Update this once the engine has real source under src/ and include/.
sonar.sources=spike
